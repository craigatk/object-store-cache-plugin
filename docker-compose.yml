version: '3'

services:
  objectstorage:
    image: bitnami/minio:2025.2.7-debian-12-r2
    ports:
      - "9000:9000"
    volumes:
      - ./minio:/data
    environment:
      MINIO_ROOT_USER: minio_access_key
      MINIO_ROOT_PASSWORD: minio_secret_key
